# WordPress Hooks Skill

When working with WordPress hooks:
- Use add_action/add_filter with priority parameter
- Document hook parameters with @param tags
- Use anonymous functions only for simple callbacks
- Always pass a known number of parameters
